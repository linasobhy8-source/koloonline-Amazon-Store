# node.js.yml
name: Node.js CI/CD - Koloonline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ سحب الكود من GitHub
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ إعداد Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm' # تسريع تحميل الباكدجات

      # 3️⃣ تثبيت الباكدجات
      - name: Install dependencies
        run: npm install

      # 4️⃣ فحص الكود (Syntax & Lint)
      - name: Run lint and syntax check
        run: |
          npm run lint || echo "Lint check failed but continue"
          node -c server.js || echo "Syntax OK"

      # 5️⃣ تشغيل السيرفر لفحصه ديناميكي
      - name: Run server for test
        run: |
          node server.js &
          sleep 5
          curl -f http://localhost:3000 || exit 1

      # 6️⃣ Build مشروع React / Next.js إذا موجود
      - name: Build frontend
        run: |
          if [ -f package.json ]; then
            npm run build || exit 1
          fi

      # 7️⃣ Deploy تلقائي إلى Vercel (مثال)
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v21
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-args: "--prod"
          working-directory: .
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}

      # 8️⃣ Test post-deploy endpoint
      - name: Test deployed API
        run: |
          curl -f https://koloonline-store.vercel.app || exit 1
